{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.saas-core.local/site-blocks/core.link_list.v1.schema.json",
  "title": "Core link list block v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["links"],
  "properties": {
    "title": {
      "type": "string",
      "maxLength": 120
    },
    "text": {
      "type": "string",
      "maxLength": 600
    },
    "layout": {
      "type": "string",
      "enum": ["buttons", "icons", "cards", "list", "split", "band"],
      "default": "buttons"
    },
    "links": {
      "type": "array",
      "minItems": 1,
      "maxItems": 12,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["label", "href"],
        "properties": {
          "label": {
            "type": "string",
            "minLength": 1,
            "maxLength": 80
          },
          "href": {
            "type": "string",
            "maxLength": 500,
            "pattern": "^(?:/(?!/)|https://|mailto:|tel:)"
          },
          "description": {
            "type": "string",
            "maxLength": 240
          }
        }
      }
    }
  }
}
